section
  if (errors.length > 0)
    for error in errors
      li.errorMsg error
fieldset
  label(for="title") Title
  br
  input(type = "text", id="title", name="title")
  br
  br
  label(for="genre") Genre
  br
  input(type = "text", id="genre", name="genre")
  br
  br
  label(for="cover_url") Cover Image URL
  br
  input(type = "text", id="cover_url", name="cover_url")
  br
  br
  label(for="description") Description
  br
  textarea.textDescription(id="description", name="description")
  br
  br
  label(for="authors") Authors
  br
  textarea.textAuthors(id="authors", name="authors")
  if admin
    section.sectionAuthorRemove
      for author in authors
        button.btnRemove Remove      
  select(name="authors" )
    for author in authors
      option(value="#{author.id}") #{author.first_name} #{author.last_name}
  button.btnAuthor Add Author
  br
  br
  
  input(type = "submit", value = "Add Book")
